box: keithyokoma/wercker-container-android

build:
  steps:
    - script:
        name: run gradle assembleDebug
        code: |
          ./gradlew --project-cache-deir=$WERCKER_CACHE_DIR assembleDebug